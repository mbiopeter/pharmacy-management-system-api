/**
 * Singleton reference to an object that has no properties.
 * Whenever one of the functions of this library returns an
 * empty object, it returns a reference to this instance.
 */
export default Object.freeze({});
