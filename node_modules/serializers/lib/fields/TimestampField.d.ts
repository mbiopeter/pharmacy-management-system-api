import deserializeDateTime from '../datetime/deserializeDateTime.js';
import type Field from './Field.js';
export default class TimestampField implements Field<Date, number> {
    readonly type: string;
    private readonly numberField;
    validate(value: Date): Date;
    serialize(value: Date): number;
    deserialize: typeof deserializeDateTime;
    encode(value: Date): string;
    decode(value: string): Date;
}
