import IntegerField from './IntegerField.js';
export default class ConstantField<Option extends number> extends IntegerField<Option> {
    private options;
    constructor(options: Option[]);
    protected validateNumber(value: number): Option;
}
