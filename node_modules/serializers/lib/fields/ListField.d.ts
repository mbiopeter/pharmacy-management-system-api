import type Field from './Field.js';
export default class ListField<I, O> implements Field<I[], O[]> {
    readonly field: Field<I, O>;
    readonly type: string;
    constructor(field: Field<I, O>);
    validate(items: I[]): I[];
    serialize(items: I[]): O[];
    deserialize(items: unknown): I[];
    encode(value: I[]): string;
    decode(value: string): I[];
    private mapWith;
}
